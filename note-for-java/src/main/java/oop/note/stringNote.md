# string

- 搜索
- 去除空白
- 比较
- 去除首尾空白字符串
- 替换子串

# StringBuilder
链式操作

```
// 链式操作的return 返回是this
    super.append(str);
        return this;
       StringBuilder sb = new StringBuilder("hello");

        sb.append(", tian").append(" love");

```
